
Работа CI ( в каждой папке CI создаеться класс с названием  расширением как имя попки если это папка controllers то в файлах в нутри нее класс называют так ( class First extends CI_Controller  где first имя файла но пишем его с большой буквы)
Контролеры (controllers) вызывают файлы из папки views (вид страници HTML разметка)


в папке config/conig.php  путь к корневой папке 
$config['base_url'] = 'http://demo/cilessons/';  - если локально
$config['base_url'] = 'http://bogdandvinin.zzz.com.ua/'; - если на хостинге указать хостинг


в файле config/autoload/php нада указать 
$autoload['helper'] = array('url','form'); - что-бы заработала функция base_url();
$autoload['libraries'] = array('database','pagination'); - НЕЗАБЫВАТЬ ПОДКЛЮЧАТЬ БИБЛИОТЕКИ С которыми нада работать на проэкте




---------------------------------Codeigniter

-------------ДЛЯ СТАРТА Нашего контролера в папке controllers нада зайти в папку config
и открыть файл routes.php и установить в массив $route['default_controller'] = 'first/articles'; где first этой файл first.php в папке controllers а articles это функция в классе First файла first.php
подключили эту функцию потому что мы щас с ней работаем с базой данных (например)

 1) (вид страници)В папке aplication/controllers  - Храняться файлы HTML которые отображают вид страници
 
 2)(все что связано с БД) в папке aplication/models
 
 3)(то что видим на странице) храниться в папке application/views
 
 4)(библиотеки хранить) application/libraries
 
 
 
 
 В папке system находятся все библиотеки готовые которые мы будем подгружать
 
 1) Настройки по соединению с БД application/config/database.php
 
 2) При создании нового проэкта нада указть путь к корневой папке в файле config -  application/config/config.php
    + 
	еще в файле config нада установить ключь если мы будем использовать Session класс
	в строке $config['encryption_key'] = 'cilessons'; (можно установить любой)
	+
	можно еще выставить что-бы в пхп можно было писать сокращенные команды 
	$config['rewrite_short_tags'] = TRUE -если TRUE поставить
	
 3) Папка с контролером (НАЗВАНИЕ ФАЙЛА С КОНТРОЛЕРОМ создаем с маленькой буквы а класс вызываемый внутри контролера с большой ) который отображает данные на нашей странице 
	application/config/controllers он загружает файлы которые мы указываем а эти файлы содержаться
	в папке application/config/views
	
	
	
	
	
--------------------------------ПРИМЕР РАБОТЫ 


 1) Создадим новый контролер в папке aplication/controllers назовем его first.php
 2) Загружаем виды из папки Views вызываем в классе из файла first в папке controllers
	Название фйлов в папке views пишеться через _ прочерк bogdan_views.php что-бы проше было найти

	
	
	
-----------	
	-Передавать параметры из функции в виды например которые вызываем в функции
	
	class First extends CI_Controller { //файл с маленькой буквы а класс вызываем с большой (такое же название пишем что и сам файл)

    public function index() //(эта функция загружаеться сама если вызвать класс First )сюда подгружаем виды из папки views
    {                      //в нашем случае класс First вызываеться из командной строки так http://demo/cilessons/index.php/first и инфо которая содержиться в функции index загружаеться автоматически
        $this->load->view('hello_views'); //load (означает загрузку айла) hello_views (имя файла который загружаем)
    }
    function about(){ // вид который содержиться в функции about подгружаеться так http://demo/cilessons/index.php/first/about
        $data['name'] = "Дмитрий";
        $data['surname'] = "Иванов";
        $data['age'] = 29;
        $this->load->view('about_views',$data);//передаем массив $data теперь можно при вызове этой функции обращаться на прямую к именам ячеек массива и они будут выводиться //создали новый вид в папке views
    }
}

   а в файле about_views можно обратиться к ячайкам массива $date используя имена ключей
   
   <body>
	<?= $name; ?>
	<?= $surname; ?>
	</body>
	
-----------	
   -Можно еще передавать значение в функцию и в функции добавлять значение на это условие
   и тогда выведеться и то что вызывает класс то что содержиться в about_views 
   и то что мы написали в условии если передадим соотвецтвующее значение в класс
   если вызовим так http://demo/cilessons/index.php/first/about/1
    то сработает условие в классе а если будет не 1 то не сработает 
   
   class First extends CI_Controller { //файл с маленькой буквы а класс вызываем с большой (такое же название пишем что и сам файл)

    public function index() //(эта функция загружаеться сама если вызвать класс First )сюда подгружаем виды из папки views
    {                      //в нашем случае класс First вызываеться из командной строки так http://demo/cilessons/index.php/first и инфо которая содержиться в функции index загружаеться автоматически
        $this->load->view('hello_views'); //load (означает загрузку айла) hello_views (имя файла который загружаем)
    }
    function about($id){ // вид который содержиться в функции about подгружаеться так http://demo/cilessons/index.php/first/about
        $data['name'] = "Дмитрий";
        $data['surname'] = "Иванов";
        $data['age'] = 29;
        $this->load->view('about_views',$data);//передаем массив $data теперь можно при вызове этой функции обращаться на прямую к именам ячеек массива и они будут выводиться //создали новый вид в папке views
        if ($id = 1){
            echo "Привет";
        }
    }
}

   
   
-----------