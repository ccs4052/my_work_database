


			Передача массива с данніми в наш вид ( в папку с HTML кодом )
			
	Также можно передать картинку например
 
 1)в папке controllers создаем файл first.php
 
 <?php
defined('BASEPATH') OR exit('No direct script access allowed'); // BASEPATH путь к корневой папке мы указали в файле config
//BASEPATH - http://demo/cilessons/index.php/
class First extends CI_Controller { //файл с маленькой буквы а класс вызываем с большой (такое же название пишем что и сам файл)

    public function index() //(эта функция загружаеться сама если вызвать класс First )сюда подгружаем виды из папки views
    {                      //в нашем случае класс First вызываеться из командной строки так http://demo/cilessons/index.php/first и инфо которая содержиться в функции index загружаеться автоматически
        $this->load->view('hello_views'); //load (означает загрузку айла) hello_views (имя файла который загружаем)
    }
    function about(){ // вид который содержиться в функции about подгружаеться так http://demo/cilessons/index.php/first/about
        $data['name'] = "Дмитрий";
        $data['surname'] = "Иванов";
        $data['age'] = 29;
        $this->load->view('about_views',$data);//передаем массив $data теперь можно при вызове этой функции обращаться на прямую к именам ячеек массива и они будут выводиться //создали новый вид в папке views
    }
}
------------ Масив дата передеться в файл about_views.php в папке views 
и в этой папке вызываеться массив с данными вот так!

<body>
<?= $name; ?>
<?= $surname; ?>

<p>Новый вид</p>

</body>
</html>



----------------------
----------------------
	Можно передавать в функцию параметры (не один много можно)
	
class First extends CI_Controller { //файл с маленькой буквы а класс вызываем с большой (такое же название пишем что и сам файл)

    public function index() //(эта функция загружаеться сама если вызвать класс First )сюда подгружаем виды из папки views
    {                      //в нашем случае класс First вызываеться из командной строки так http://demo/cilessons/index.php/first и инфо которая содержиться в функции index загружаеться автоматически
        $this->load->view('hello_views'); //load (означает загрузку айла) hello_views (имя файла который загружаем)
    }
    function about($id){ // вид который содержиться в функции about подгружаеться так http://demo/cilessons/index.php/first/about
        $this->load->view('about_views');//передаем массив $data теперь можно при вызове этой функции обращаться на прямую к именам ячеек массива и они будут выводиться //создали новый вид в папке views
        if ($id == 1){
            echo "Привет";
        }
    }
}

выведиться привет если в условие передастса 1 
http://demo/cilessons/index.php/first/about/1 - контролер first функция about передаем 1 то сработает условие

------------------------	